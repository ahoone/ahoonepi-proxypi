[project]
name = "proxypi-cli"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "typer==0.27.1",
]

[project.scripts]
proxypi = "proxypi.main:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
