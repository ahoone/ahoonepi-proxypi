services:
  fastapi:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - path: ../config.env
        required: true
    ports:
      - "${HTTP_PORT_BROKER}:8000"
    volumes:
      - ./:/app
      - ../plugins:/plugins:ro
      - /tmp/proxypi.sock:/tmp/proxypi.sock
    environment:
      NODE_ROLE: ${NODE_ROLE}
    restart: unless-stopped
    networks:
      - default

networks:
  default:
